import React from 'react';
import { ScrollView, Text } from 'react-native';

const TodayEnv = () => {
    return (
        <ScrollView>
            <Text>오늘의 환경 페이지 입니다.</Text>
        </ScrollView>
    );
}

export default TodayEnv;